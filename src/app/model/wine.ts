export interface Wine {
    id_wine: number,
    namewine: string,
    is_hidden: boolean | null,
    created_date: Date,
    codewine: String,
    effect: string,
    img: string | null,
    img1: string | null,
    img2: string | null,
    img3: string | null,
    img4: string | null,
    volumewine: string,
    otp: string,
    ginsnegId: number,
    ginsengCode: string
}
